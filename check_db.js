import admin from 'firebase-admin';

// Initialize Firebase Admin (assuming you have a service account or can connect locally if emulators are running, wait, this is a Nuxt app using Firebase Client SDK)
